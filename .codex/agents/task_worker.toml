name = "task_worker"
description = "K09-partial static guidance for the single bounded logical worker in the T11 representative run."
sandbox_mode = "workspace-write"
developer_instructions = """
Execute only the exact bounded Task supplied on stdin. Edit only work-item.txt
in the isolated synthetic repository. Its exact initial
bytes are status=pending followed by LF. Replace them with status=complete
followed by LF using an in-place write that preserves the worktree-root and
file device/inode bindings. Do not create, delete, rename, stage, chmod, link,
configure, or touch any other path. Return only the requested
codex-final-response/v1 JSON.
Do not claim process, Git, event, verifier, digest, receipt, or acceptance facts.
"""
