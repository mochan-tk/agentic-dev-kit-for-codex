# Bounded source-parity disposition

This is a behavior-level comparison with Copilot source
[`446071c76f14f5fbda37a0eef1b6eafa0a3ab897`](https://github.com/mochan-tk/agentic-dev-kit-for-copilot/tree/446071c76f14f5fbda37a0eef1b6eafa0a3ab897),
not a percentage complete or a claim that every source filename, client or
runtime feature must be copied. Accepted product outcomes remain historical;
new candidate evidence and owner acceptance are distinct.

| Audit ID | Current implementation and disposition | Evidence and remaining boundary |
| --- | --- | --- |
| G01 | Explicit three-file adopter CI addon reaches the installed Task ritual. | Accepted public product PR #9; real local tools and synthetic GitHub. Not automatic installation or live rollout. |
| G02 | Explicit single-maintainer governance profile; startup distinguishes tuned, untuned and observation failure. | Accepted public product PR #7; scoped owner decline and fail-closed sensor regressions. No inferred consent or live setting change. |
| G03 | Supervisor/worker responsibility, current-plan acknowledgement and stop/disposition procedures. | Shipped procedures plus synthetic contracts; historical adopter use is not current-version native client discovery or termination proof. |
| G04 | Installed ritual render and GET-only preflight, exact membership and final readbacks. | Accepted PR #7, including duplicate-key and long-body corrections in numeric PR mode. No comment publication or durable-write guarantee. |
| G05 | Metadata/code isolation and retarget freshness in the opt-in addon. | Accepted PR #9 and named regression tests. Not every latest-base advance or every CI form; see [addon limits](distribution/adopter-ci.md). |
| G06 | Base/head fixed control inventory and supported workflow-form drift checks, deliberately tighter than source line-presence/waiver semantics. | Accepted PR #9. No arbitrary workflow support, automatic Ruleset change or immutable security boundary. |
| G07 | Explicit retrospective reports, per-file budgets and official HTML checkpoints. | Accepted public product PR #11; synthetic transport. Templates remain inert; HTML differences are not semantic platform capability judgments. |
| G08 | Public manual form and [pinned two-file no-clone sender](distribution/feedback.md), default draft with explicit terminal-consented send. Installer failures give fixed form/guide links. | Task #16 delivery uses unchanged accepted sender/library bytes; real Bash/PTY and synthetic HTTP/GitHub regressions. The deliberately manual failure handoff is not the source's automatic interactive offer. Accepted PR #11 receiving helpers remain explicit; no active receiver or schedule, live submission, E01 result or owner acceptance of full parity is claimed. |
| G09 | Invocation-local shared-blocker state cache, canonical host/repository/Issue key, Bash 3.2 arrays, validated OPEN/CLOSED values only. | Candidate public product Task #12: real Bash/jq with synthetic transport compares accepted-old 152 total calls / 100 state reads with 54 / 2 for fifty Tasks sharing two blockers. This is a deterministic call-count test, not network or wall-clock performance. |
| G10 | The adopter retains the installed source revision manually and supplies exact old/new IDs to preview/apply. | Deliberate supported difference from source automatic version recording. No version registry, auto-detection, scheduling, arbitrary-version migration or silent customization merge is claimed. See [update contract](distribution/source-first-installer.md). |
| E01 | Current-version minimal client/worker/Task/adopter-CI/handoff cycle. | **Pending** separate authority and actual measurements. Past use, role/Skill files and synthetic tests cannot establish this result. Record the actual client and explicit installed-file loading versus native discovery when measured; do not manufacture native role selection. |
| E02 | Native Windows end-to-end installation and operation. | **Unmeasured**, separately deferred. PowerShell-host synthetic tests and macOS/Linux evidence do not become Windows success. Universal runtime is neither claimed nor silently added to this bounded package. |

The cache reuses one successfully read state per identity within a process;
each new invocation refetches. Dependency membership is still validated for
every Task. An OPEN blocker does not skip later observations. Unknown, failed,
incomplete or inconsistent data refuses the entire result before a partial
frontier can be printed. This is not an atomic GitHub snapshot or lock.

Only the frontier engine and its exact ritual adoption-anchor compatibility
change in this package; all 47 payload paths/classes/modes and the other 45
contents remain unchanged. The separate adopter-CI sensor's supported ritual
digest is synchronized, without changing its workflow semantics or migrating
an installed addon. Its deployment and guarded-file transitions remain explicit.
The legacy 47-file updater does not update an existing addon's old sensor or
configuration digest; setup refuses non-identical existing addon files. Those
installations therefore need a separate owner-reviewed transition. A fresh
candidate deployment would not prove existing-addon migration. Inert improvement
templates also cannot simply be added to the addon's closed workflow inventory.

Acceptance of implementation, synthetic evidence, historical adopter use and
current live measurement must be recorded separately. No blanket full-parity,
product-final or release acceptance follows from this table. T12 remains paused,
`release_blocked=true` remains in force, and the original 136 scenarios stay
`not-run` with `results: []`. No existing-adopter, service, schedule or runtime
operation is authorized by this document.
